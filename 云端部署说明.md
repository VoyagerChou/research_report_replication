# Meta MASTER 云服务器部署包（README 结构版）

> 用途：把本地已有代码按实习生 README 的工程结构排布成一套完整可跑的 Meta MASTER 管线，
> 上传到 SSH 云服务器执行 `prepare → train → update → eval`，复现/验证 README 声称的成绩。
> 代码部分已全部就位并通过 import 验证；数据部分按下方指引从你本地/云端放置。

---

## 1. 目录结构（与 README 第 6 节一致）

```
meta_master_readme/                  ← 本包根目录(上传时整个文件夹传上去即可)
├── config.py                       全部超参与路径(单一事实来源)
├── main.py                         编排入口(prepare/train/update/master/eval/compare/drmcheck)
├── README.md                       原 README(交付说明)
├── requirements.txt / MASTER.pdf
├── 参数溯源审计表.md / 指标计算口径.md / 数据说明.md / tech53因子计算说明.md
│
├── model_core/                     ✅ 原样(骨干/门控/适配器/元训练器/DRM/PanelData)
├── input_data_update/              ✅ 原样(build_base/tech53/idx63/labels/drm/prepare_local_data)
├── train/                          🆕 double_adapt_train.py + __init__.py   离线元训练薄壳
├── update/                         🆕 update.py(在线元增量) + master_score.py(静态线) + __init__.py
├── evaluate/                       🆕 factor_eval.py + compare.py + diag_drm.py + __init__.py
├── tools/                          (空, __init__.py)
└── data/                           ← 数据目录(本地上传时为空, 你在云端放数据)
    ├── raw_tables/                 7 张原始表(构建 prepare 时读取)
    ├── derived/                    共享产物(build 生成: base/tech53/idx63/labels/expo/market_state/panel)
    ├── models_weights/             DRM/MASTER/DA 权重(训练生成)
    ├── factor_values/              因子落库(update/master_score 生成)
    └── eval*/                      评价产出(eval 生成)
```

🆕 = 我新写的薄壳（原版缺失）。model_core / input_data_update / config / main 均来自你本地现有代码。

---

## 2. 数据摆放指引（云端/本地都适用）

代码通过 `config.py` 定位数据，路径 = `<包根>/data/...`。请把数据放到 data/ 下的对应子目录：

| 你要放的数据（你本地对应物） | 放到云端哪 | 说明 |
|---|---|---|
| 日行情(开高低收/量额/vwap/复权/停牌) | `data/raw_tables/stock_daily/stock_daily_2*.pq` | 我本地已从 data_20260908 转换好，可直接拷 |
| 换手率衍生 | `data/raw_tables/stock_derivative/*_2*.pq` | 同上 |
| Barra CNE5 十风格暴露 | `data/raw_tables/barra_exposure/*_2*.pq` | 同上（code 已归一 .SZ/.SH） |
| Barra 特质收益 | `data/raw_tables/barra_special_returns/*_2*.pq` | 同上 |
| 指数日行情(300/500/1000/800) | `data/raw_tables/index_daily/index_daily_all.pq` | 同上 |
| ST 区间 | `data/raw_tables/stock_st/stock_st_all.pq` | 同上（从 is_ST 反推的区间） |
| 指数 PIT 成分进出日 | `data/raw_tables/index_members/index_members_all.pq` | 同上（从成分标志反推） |

> 如果你云端本来就有 README 配套的原始表（实习生那份 data/raw_tables），**直接用云端的**，格式已对（列 date/code/open... + limit_up/limit_down + index_members PIT），不用我本地转的这份。

**最省事的上传方式**：直接把本地 `E:\zxjt\Meta MASTER\data\raw_tables\`（我已转好的 7 个子目录）整个传上去放 `data/raw_tables/`。构建产物（derived 等）不需要传，`prepare` 会自动生成。

---

## 3. 云端执行顺序

```bash
cd meta_master_readme
pip install -r requirements.txt          # torch/numpy/pandas/pyarrow/tqdm/matplotlib

python main.py prepare      # ① 数据准备: base→tech53→idx63→backtest_aux→labels→DRM→panel (DRM 是 GPU 重活, 2-7h)
python main.py train        # ② 离线元训练 (φ冷启动 + ψ, 2018轨迹验证早停, da_offline.pt)
python main.py update       # ③ 在线元增量 2019-2026, 每20日, 落 factor_values/meta_master_score/
python main.py eval         # ④ 评价主线因子 (RankIC + 多头超额)

# 对照线(可选, 可另开一张卡并行)
python main.py master       # 静态 MASTER 2019-2024 逐年训练+打分
python main.py eval master
python main.py compare      # 横向对照表
python main.py drmcheck     # DRM 验收
```

---

## 4. 我新写薄壳的诚实说明（重要）

以下三个模块原版缺失（本地、云端都没有），我按 README + model_core 已有逻辑补写：

| 模块 | 说明 | 与 README 可能的差异 |
|---|---|---|
| `train/double_adapt_train.py` | 载入 PanelData → `DoubleAdaptTrainer.train_offline()` | 核心循环全在 model_core，薄壳仅编排，风险低 |
| `update/update.py` | 载 da_offline → 每20日 outer_step+adapt_delta+score_days → 按年落库 | 断点续跑粒度=整年（原版可能是节点级快照），重跑整年仍逐日一致 |
| `update/master_score.py` | 逐年 `train_master_year` + 逐日打分落库 | 打分用 panel.day_batch，与原版一致 |
| `evaluate/factor_eval.py` | **简化版**：周频RankIC/ICIR + 简化双周G1超额 | **不是完整回测**（无收盘盯市/涨跌停冻结/精确换手）。完整复刻在 `指标计算口径.md`，量大可按需再补 |
| `evaluate/compare.py`, `diag_drm.py` | 薄汇总/诊断 | 低风险 |

**因此：跑出来的 RankIC/ICIR（不依赖回测口径）可对标；多头超额请以你自己或后续补的完整回测为准。** 若你验证后 RankIC 接近 README 的 0.1145，说明因子本体复现成功，超额差异主要来自评价口径（README 自己列了对标差异表）。

# 指标计算口径说明

> 实现见 `evaluate/factor_eval.py`，常数见 `config.py` 的 `bt_*` / `ic_*` 键。

## 基础约定

**时点**：因子 `date=t` 含 t 日收盘信息，最早 **t+1 按 vwap 建仓、t+11 平仓**（持有 10 日）。

**频率**：调仓双周（隔周周五信号），但收益与风险按**日频**核算（收盘盯市，换仓日拆腿：
旧仓 close(t)→vwap(t+1) 卖出、新仓 vwap(t+1)→close(t+1) 买入，费用计在当日）。
例外：多空、期胜率、换手定义在调仓期上，按双周口径。

**区间端点**：截取某端点（如研报端点 2025-01-06）时，日频类指标截至该日**收盘**；调仓期类
指标以**信号日 ≤ 端点**为准，末期仓位持有至平仓日（2025-01-06 对应末期信号 2025-01-03、
平仓 2025-01-20）完整计入——期是最小核算单位，不可腰斩。
注意 `run_bt(end=)` 过滤的是信号日，其返回的日频序列会延伸到末期平仓日，**对齐端点须再截断**
（该 10 日恰逢 2025 年 1 月反弹，不截断则全A Top10% 超额由 19.50% 虚增至 20.51%）。

**年化基数**：日频序列 ×√243 / 双周序列 26 期 / 周频 IC ×√52。

**组合**：全 A 池（非 ST ∧ 有成交 ∧ 输入窗完整 ∧ 上市满 60 交易日）→ 分十组 →
**G1 等权满仓** → T+1 vwap 成交 → 单边 1.5‰ → 涨停不可买、跌停冻结。
池内评价为同一因子筛该指数 PIT 成分后**池内重排**，对标该指数。

## 指标定义

| 指标 | 公式 | 说明 |
|---|---|---|
| 绝对收益 | `[Π(1+r_p,t)]^(243/n) − 1` | 日频连乘后年化 |
| **超额收益** | `excess_t = r_p,t − r_b,t`；`[Π(1+excess_t)]^(243/n) − 1` | **日频算术差先相减、再连乘** |
| 超额波动 | `std(excess_t) × √243` | |
| **IR** | `mean(excess_t) / std(excess_t) × √243` | 分子用**算术均值**，故 `IR ≠ 超额收益/超额波动` |
| 夏普 | `mean(r_p,t) / std(r_p,t) × √243` | **rf = 0** |
| 超额MDD | 日频超额净值曲线的 `min(NAV/cummax(NAV) − 1)` | 分年行在该年区段内量 |
| 多空 | `[Π(1 + r_G1,k − r_G10,k)]^(26/K) − 1` | 调仓期口径，**不扣费** |
| 期胜率 | 超额为正的调仓期占比 | |
| 换手 | `mean(tv_k/2) × 26`，`tv_k = Σ|目标权重 − 漂移后权重|` | **单边**；只计主动调整，不含价格漂移 |
| RankIC | 每周 W-FRI 末日截面 `Spearman(score, 未来10日vwap收益)` | 对**原始收益**而非排序标签；截面有效样本 ≥2 即计算（池内评价由 PIT 成分掩码保证截面充足） |
| 年化ICIR | `mean(RankIC) / std(RankIC) × √52` | |
| IC胜率 | RankIC > 0 的周数占比 | |

**分年 vs 全样本**：分年行是区间累计 `Π(1+r) − 1`（不年化）；全样本行重新年化（CAGR）。
两者不可混用，逐年对标只能年度行对年度行。

## 对标研报时须注明的差异

| 项 | 复现 | 研报 |
|---|---|---|
| 超额算法 | 日频算术差复利 | **比值法**（判据：其表1 中 2022 年组合 25.6% 而超额 56.3%），复现同口径 +1.9pp |
| MDD 汇总行 | 全期日频曲线回撤 | **最差年度值**（其汇总行与 2024 行逐字相等），定义不同须分别报 |
| 夏普 rf | 0 | 未披露；若其扣 2%，复现偏高约 0.1 |
| 区间端点 | 可截至 2025-01-06 对齐 | 截至 2025-01-06 |

**RankIC / ICIR / 多空 / 期胜率 / 换手五项不依赖上述口径，可直接对标。**

> 关于回撤：复现回撤更深**主要是业绩差异而非量法差异**——MDD 缺口与超额缺口高度正相关
> （Pearson +0.854 / p=0.031，n=6），且重采样到双周后各年并未系统性收敛（2020 仍差 6.9pp、
> 2021 反而更浅 3.3pp）。**应作为业绩差距如实报告，不可用口径解释掉。**

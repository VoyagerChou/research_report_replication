# 落盘数据规格

**维度记号**：**T**=交易日数（4260，2009-01-05 ~ 2026-07-20）、**J**=股票数（5472，全历史 A 股并集，剔 .WI/.BJ）、**F**=每个"股票×日期"格子上的特征数。例：`tech53.dat` 形状 (T,J,53) 中 `[t,j,k]` = 第 j 只股票在第 t 个交易日的第 k 个因子值。所有矩阵共用 `base.npz` 的 `DATES`/`CODES` 坐标轴，可直接按下标对齐，无需 join。

**约定**：float 用 NaN 表示"该股当日无此数据"；`.dat` 是 .npy 格式（含 128 字节头），**读写必须用 `np.lib.format.open_memmap`**，裸 `np.memmap` 会错位 32 个 float32；共享大件均原子写（临时名→`os.replace`）；实验变体产物自动加后缀（`_drmdk` 等，见 config.py 的 ATAG），定稿无后缀。

## data/raw_tables/ — 原始表（7 张，约 1.3G）

`stock_daily`(日行情/涨跌停价) · `stock_derivative`(换手率等衍生) · `barra_exposure`(CNE5 暴露)
· `barra_special_returns`(特质收益) · `index_daily`(指数行情) · `index_members`(PIT 成分)
· `stock_st`(ST 区间)。仅 `python main.py prepare` 从零重建 derived 产物时读取；
日常训练/推理/评价不触碰。

## data/derived/ — 共享产物

| 文件 | 大小 | 内容 |
|---|--:|---|
| `base.npz` | 1.5G | `PRICE`(T,J,5)=开高低收量（OHLC 后复权+ffill）· `BARRA`(T,J,10)=CNE5 暴露 · `RET` 日收益 · `VALID` 可投掩码（非ST ∧ 有成交 ∧ 窗口完整 ∧ Barra 新鲜）· `LABEL_OK` 未来 20 日完整 · `OBS` 原始有行情行 |
| `tech_arrays.npz` | 370M | `AMT` 成交额 · `VWAP_H` 后复权 vwap · `TURN` 换手率(%) · `SPEC` Barra 特质收益 |
| `tech53.dat` + `_stats` | 4.9G | (T,J,53) 技术因子原始值；`_stats` 存逐日全市场截面的均值/标准差（z 化基准）。算法见 `tech53因子计算说明.md` |
| `idx63.npz` | 1M | (T,63) 指数市场状态：300/500/1000 各 21 维 |
| `backtest_aux.npz` | 115M | `BUYABLE`/`SELLABLE` 涨跌停掩码 · `BENCH_{池}` 指数点位 · `MEMBER_{池}` PIT 成分掩码 |
| `bench_csi_allshare.csv` | 68K | 中证全指（000985.CSI）点位，供基准口径对照 |
| `labels.npz` | 210M | `Y_RAW`=`VWAP_H[t+11]/VWAP_H[t+1]−1` · `LBL_OK` 建仓与平仓日均真实成交 · `Y_RANK` 排序池内截面 rank 归一至 [0,1] |
| `deep_risk_exposure_daily/` | — | 按年分片长表：`date` `code` `d0`…`d9`（截面 z 后的 10 维风险暴露） |
| `expo.dat` | 0.9G | (T,J,10) 上表网格化，时间轴 ffill ≤10 日 |
| `market_state.npz` | — | (T,183) = idx63(63) + risk120(120：每日涨幅前 10% 股票的 10 因子均值 × {5,10,20,40}窗 × {mu,sd,amp}) |
| `panel.npz` | 840M | **训练/评价唯一入口**：日历 + 行情 + 标签 + 市场状态 + `MASTER_POOL`（VALID ∧ 上市满 60 日）。53 因子内容不进面板，故换因子源无需重装 |

## data/models_weights/ — 模型权重（`torch.save` 字典）

`drm_{年}.pt`（`state`/`meta`，meta 含 **warm_started**）· `master_{年}.pt`（`model`/`gate`/`gn`/`meta`）· `da_offline.pt`（`phi` 骨干+门控 / `adapter` 适配器 / `opt` / `gn` / `meta`）· `da_online_state_{年}.pt`（含 `rng`，断点续跑与不中断逐位一致）· `*_resume.pt` 断点存档（完成后自动删）。`gn` 统一为 `{med:(183,), mad:(183,)}`。

## data/factor_values/ — 因子值落库（parquet，按年分片）

`meta_master_score/`（**主线交付**）· `master_score/`（静态 MASTER 对照）。
列：`date` · `code` · `score` float32 · `model_version`；主键 `(date, code)`。
入库映射（公司因子库四列标量长表）：`date→date` · `code→instrument` · `score→factor_value` ·
`factor_id`=入库时分配；`model_version` 不入库仅留档。

> **时点约定**：`date` 是**信号日**、含当日收盘信息，**不可用于当日成交**；最早 t+1 按 vwap 建仓、t+11 平仓。score 仅截面可比。

## data/eval*/ — 评价产出

`eval_{池}`（主线）/ `evalM_{池}`（MASTER 线）/ `eval-{变体}_{池}`，池 ∈ {allmkt, csi300, csi500, csi1000}。
每目录含 `ic_summary/ic_by_year/ic_weekly.csv`、`bt_yearly/bt_weekly/bt_daily.csv`、`nav/decile/rankic.png`。
指标定义见 `指标计算口径.md`。
